<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chỉnh sửa tin tức du lịch</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      font-family: Arial, sans-serif;
    }
    .container {
      margin-top: 50px;
    }
    .form-group {
      margin-bottom: 15px;
    }
    .form-control {
      border-radius: 10px;
    }
    .btn-primary {
      background-color: #007bff;
      border-color: #007bff;
    }
    .btn-primary:hover {
      background-color: #0056b3;
      border-color: #0056b3;
    }
  </style>
</head>
<body>
  <div class="container">
    <h2 class="text-center mb-4">Chỉnh sửa tin tức du lịch</h2>
    <form action="/travel-news/edit/<%= travelNews._id %>" method="POST">
      <div class="form-group">
        <label for="title">Tiêu đề:</label>
        <input type="text" id="title" name="title" class="form-control" value="<%= travelNews.title %>" required>

        <label for="imageUrl" class="mt-3">Ảnh:</label>
        <input type="text" id="imageUrl" name="imageUrl" class="form-control" value="<%= travelNews.imageUrl %>">

        <label for="date" class="mt-3">Ngày:</label>
        <input type="text" id="date" name="date" class="form-control" value="<%= travelNews.date %>">
      </div>

      <button type="submit" class="btn btn-primary btn-block mt-4">Cập nhật</button>
    </form>
    <a href="/travel-news" class="btn btn-link mt-3">Quay lại danh sách tin tức</a>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
